package com.u2m.app_mercadofacil

import io.flutter.embedding.android.FlutterActivity

/**
 * Entry point nativo do aplicativo no Android.
 *
 * A classe usa o embedding padrao do Flutter e, no estado atual do projeto,
 * nao adiciona customizacoes especificas da plataforma.
 */
class MainActivity : FlutterActivity()
